package lang.classes.ClassValue.WW_Doc;

// https://docs.oracle.com/javase/8/docs/api/java/lang/ClassValue.html#ClassValue--

class ClassValue_RT {
    ClassValue clsValue;
}

/*             public abstract class ClassValue<T> extends lang.classes.Object

    Lazily associate a computed value with (potentially) every type. For example, if a dynamic language needs to construct
        a message dispatch table for each class encountered at a message send call site, it can use a ClassValue to cache
        information needed to perform the message send quickly, for each class encountered.

    Since:
    1.7


  RELATED


 */

