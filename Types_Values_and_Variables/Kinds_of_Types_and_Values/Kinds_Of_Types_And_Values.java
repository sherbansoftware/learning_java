package Types_Values_and_Variables.Kinds_of_Types_and_Values;

// https://docs.oracle.com/javase/specs/jls/se8/html/jls-4.html#jls-4.1

/*                                                     The Kinds of Types and Values

    There are two kinds of types in the Java programming language:

            * primitive types
            * reference types

    There are, correspondingly, two kinds of data values that can be stored in variables, passed as arguments, returned
       by methods, and operated on:

            * primitive values
            * reference values

    There is also a special null type, the type of the expression null , which has no name.
    Because the null type has no name, it is impossible to declare a variable of the null type or to cast to the null type.
    The null reference is the only possible value of an expression of null type.
    The null reference can always be assigned or cast to any reference type (§5.2, §5.3, §5.5).
    In practice, the programmer can ignore the null type and just pretend that null is merely a special literal that can
        be of any reference type.


 */

public class Kinds_Of_Types_And_Values {
}
